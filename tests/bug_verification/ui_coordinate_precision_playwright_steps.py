"""
Playwright steps executed via mcp_browser_automation for coordinate precision UI verification.
This artifact documents the browser test flow: login, inspect create/edit latitude-longitude
attributes, verify real comma-separated paste behavior, verify list precision, and verify map popup.
"""
# See /app/test_reports/bug_verification_17.json for results. The actual executed script was passed
# to the browser automation tool because the tool runs inside its own async page context.
